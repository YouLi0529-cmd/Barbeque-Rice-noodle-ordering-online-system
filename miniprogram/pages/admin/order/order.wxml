<!--pages/admin/order/order.wxml-->
<view class="order-admin-page">
  <view class="top-bar fire-gradient">
    <view class="top-title">订单管理</view>
    <view class="top-desc">堂食、户外、付款状态统一查看</view>
  </view>

  <view class="filter-panel">
    <scroll-view class="filter-scroll" scroll-x enhanced show-scrollbar="{{false}}">
      <view class="filter-row">
        <view
          class="filter-chip {{activeFilter === item.value ? 'active' : ''}}"
          wx:for="{{filters}}"
          wx:key="value"
          bindtap="onFilterChange"
          data-value="{{item.value}}">
          {{item.label}}
        </view>
      </view>
    </scroll-view>
    <view class="refresh-line">
      <view class="refresh-btn" bindtap="refreshOrders">刷新</view>
      <text>每 10 秒自动刷新</text>
    </view>
  </view>

  <view class="order-card" wx:for="{{orders}}" wx:key="_id">
    <view class="card-head">
      <view>
        <view class="order-kind">{{item.orderKindText}}</view>
        <view class="order-time">{{item.createTimeText}}</view>
      </view>
      <view class="status-tag">{{item.statusText}}</view>
    </view>

    <view class="recharge-info" wx:if="{{item.type === 'recharge'}}">
      <view class="info-row">
        <text>充值金额</text>
        <text>¥{{item.amount}}</text>
      </view>
      <view class="info-row">
        <text>赠送金额</text>
        <text>¥{{item.giveAmount}}</text>
      </view>
      <view class="info-row">
        <text>到账金额</text>
        <text>¥{{item.totalGet}}</text>
      </view>
    </view>

    <view class="goods-list" wx:if="{{item.type === 'order'}}">
      <view class="goods-row" wx:for="{{item.goods}}" wx:for-item="goods" wx:key="dishId">
        <text class="goods-name">{{goods.dishName || goods.goodsName}}</text>
        <text class="goods-count">x{{goods.count}}</text>
      </view>
    </view>

    <view class="meta-grid">
      <view class="meta-item">
        <text class="meta-label">实付</text>
        <text class="meta-value">¥{{item.finalPriceText}}</text>
      </view>
      <view class="meta-item">
        <text class="meta-label">付款</text>
        <text class="meta-value">{{item.payText}}</text>
      </view>
      <view class="meta-item" wx:if="{{item.printStatus}}">
        <text class="meta-label">小票</text>
        <text class="meta-value">{{item.printStatus === 2 ? '已打印' : item.printStatus === 3 ? '打印失败' : '未打印'}}</text>
      </view>
    </view>

    <view class="actions" wx:if="{{item.type === 'order' && !(item.pay_status || item.payStatus)}}">
      <view class="action-btn primary" bindtap="confirmOfflinePaid" data-id="{{item._id}}">确认线下已付款</view>
    </view>
  </view>

  <van-empty wx:if="{{orders.length === 0}}" description="暂无订单数据" />
</view>
